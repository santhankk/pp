name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      -name: Print gretings
        run: echo "Hello World!"
      -name: Pring goodbye
        run: echo "Done - bye!"
